package com.learning.interfaces;
// method inside interface can be abstract
//multiple inheritance is achieved through interface
public interface Sample {
    int x=10;   // it is public static final by default
    //concrete method must be static or default
    public String eat(String a);
}
